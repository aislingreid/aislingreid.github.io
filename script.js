import { WORDS } from "./words.js";

function guessCheck(j) {
  //check all boxes have been filled in
  for (var i = 0; i < 5; i++) {
  //loop through each box
    if (GjLi === "") {
      return false;
    }
    else {
      return true;
    }
}

